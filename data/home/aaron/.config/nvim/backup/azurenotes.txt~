To login to Azure  via PowerShell : Connect-AzAccount
Get help or lookup a command
Get-Command -Verb Get -Noun AzVM* -Module Az.Compute
Get-Command -Verb New -Noun AzRes* -Module Az.Resources

# Create a resource group
New-AzResourceGroup -Name APTRGCentralUS01 -Location "South Central US" -Tag @{Empty=$null; UserType="Student"; Subscription="SimpliLearn"}
# List Resource
get-AZResourceGroup -Name "APT*"
get-AZResourceGroup -Location "south central us"

# Create A new Azure Virtual Machine
 New-AzVm -ResourceGroupName "APTRGCentralUS01" -Name "testvm-eus-01" -Credential (Get-Credential) -Location "East US" -Image UbuntuLTS -OpenPorts 22
# Resource group metadata is in central US but the VM is created in East US

$vm = Get-AzVM -Name "testvm-eus-01" -ResourceGroupName "APTRGCentralUS01"
 $vm.HardwareProfile
#get IP address of VM
$vm | Get-AzPublicIpAddress

Try this command to list all the resources in your resource group:
Get-AzResource -ResourceGroupName $vm.ResourceGroupName | ft
