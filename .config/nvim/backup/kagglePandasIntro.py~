import pandas as pd
reviews = pd.read_csv(
    '~/projects/datasets/winemag/winemag-data-130k-v2.csv', index_col=0)
# magpointcounts = reviews.groupby('points').points.count()
# print(magpointcounts)
# print(reviews['points'].value_counts().index.tolist())
# print(reviews.groupby('points').price.min())
# print(reviews['points'].value_counts(sort=True))
# print(reviews['points'].value_counts(ascending=True))
# print(reviews.head)
# print(reviews.iloc[0])
# print(reviews)
print(reviews.groupby(['country', 'province']).apply(
    lambda df: df.loc[df.points.idxmax]))

# countriesReviewed = reviews.groupby(
# ['country', 'province']).price.agg([len, min, max])
# countriesReviewed.reset_index()
# print(countriesReviewed.sort_index())
reviews_written = reviews.groupby(
    'taster_twitter_handle').taster_twitter_handle.count()
reviews_written = reviews.groupby('taster_twitter_handle').size()
## print(reviews_written)
index = pd.date_range('1/1/2000', periods=8)
# print(index)
